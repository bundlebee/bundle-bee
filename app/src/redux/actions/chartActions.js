import * as types from './actionConstants.js';

export const displaySizes = () => ({
  type: types.DISPLAY_SIZES
});

export const displayFactoryTimes = () => ({
  type: types.DISPLAY_FACTORY_TIMES
});

export const displayBuildingTimes = () => ({
  type: types.DISPLAY_BUILDING_TIMES
});

